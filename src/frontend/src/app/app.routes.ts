import { Routes } from '@angular/router';

export const routes: Routes = [
  // Phase 2: { path: 'forms', loadComponent: () => import('./form-builder/form-list/form-list').then(m => m.FormListComponent) },
  // { path: '', redirectTo: 'forms', pathMatch: 'full' }
];
